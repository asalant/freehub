h1 do
  link_to @person.full_name, person_path(:id => @person)
  text " : Detail"
end

error_messages_for :person

form_for @person, :url => person_path(:id => @person) do |f|
  person_form_fields(f)
  div.row { userstamp_labeled_values @person }
  p { f.submit 'Update'  }
end

link_to 'Destroy', person_path(:id => person), :confirm => 'Are you sure?', :method => :delete
