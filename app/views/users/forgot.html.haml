%h1 User: Send password reset
div.column.left
  -form_for @user, :url => forgot_path do |f|
    -error_messages_for :user
    %ul
      labeled_input 'Email', :for => :user_email
        = f.text_field(:email)
        %p.instruct Enter the email address you signed up with. You will get an email with a link to reset your password.
      %li 
        =submit_tag 'Submit'

-# Original:

-#   0: h1 'User: Send password reset'
-#   2: div.column.left do
-#   3:   form_for @user, :url => forgot_path do |f|
-#   4:     error_messages_for :user
-#   5:     ul do
-#   6:       labeled_input 'Email', :for => :user_email do
-#   7:         text f.text_field(:email)
-#   8:         p.instruct "Enter the email address you signed up with. You will get an email with a link to reset your password."
-#  10:       li 
-#  11:         submit_tag 'Submit'
